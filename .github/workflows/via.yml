name: blank

on:
  push:

jobs:
  handful:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        wise: ["creative", "distribution", "lover", "position", "ceremony"]
        extend: ["treasure", "basin", "corridor", "suicide"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar qg
